import { NotificationLoggingInterceptor } from './notification-logging.interceptor';

describe('NotificationLoggingInterceptor', () => {
  it('should be defined', () => {
    expect(new NotificationLoggingInterceptor()).toBeDefined();
  });
});
